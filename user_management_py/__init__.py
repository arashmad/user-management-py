"""
Top-level package for FastAPI  Poetry Boilerplate.

The `user_management_py` package provides a boilerplate for
a FastAPI application.

This package includes the main application module and any other modules
that are part of the application.

The main application module is `app.py`, which sets up
the FastAPI application and defines the routes and other configuration.

Other modules in the package may include
utility functions, data models, and other components of the application.

"""
